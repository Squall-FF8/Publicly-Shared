program Test;
uses X16, VERA, VeraFX, Utils, Text;

var
  b: byte;
  w, w1, w2: word;
  d, d1: dword;

  v: [5]char;
  t, t1: triplet;
  
begin
  d := d1 + 1 + 1;
  //t := triplet(d);
  //t := t1;
  //t := triplet($34121212);
  //t := triplet($123456);
  
  {$RTS} 
END.  
